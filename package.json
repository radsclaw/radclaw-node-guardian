{
  "name": "radclaw-node-guardian",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Core Lightning monitoring and operational audit service",
  "scripts": {
    "test": "python3 -m unittest discover -s tests -p 'test_*.py' -v && node --test tests/*.test.js",
    "check": "python3 -m py_compile node_guardian/probe.py && node --check server.js && node --check public/app.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
